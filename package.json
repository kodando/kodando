{
  "name": "kodando-project",
  "version": "1.2.1",
  "author": "Daniel Ferreira Monteiro Alves <danfma@gmail.com>",
  "license": "MIT",
  "scripts": {
    "release:next": "changelog -m && git add CHANGELOG.md && git commit -m 'Updated CHANGELOG.md' && yarn version --minor && git push origin && git push origin --tags",
    "release:minor": "changelog -p && git add CHANGELOG.md && git commit -m 'Updated CHANGELOG.md' && yarn version --patch && git push origin && git push origin --tags",
    "test": "jest"
  },
  "dependencies": {
    "kotlin": "^1.2.20",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-router-dom": "^4.2.2",
    "rxjs": "^6.1.0"
  },
  "devDependencies": {
    "generate-changelog": "^1.7.1",
    "jest": "^23.0.0",
    "jest-sourcemaps": "^1.0.1",
    "shelljs": "^0.8.2"
  }
}
